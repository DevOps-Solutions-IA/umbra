package app.umbra.ui.model;

/** Semantic tone of a state. Always rendered together with a {@link Glyph} and a text label. */
public enum Tone { NEUTRAL, ACCENT, SUCCESS, WARNING, DANGER, OFFLINE, VERIFIED, IDENTITY, BLOCKED }

# P0-01 — Android real, backend y contenedor

VERIFIED — Sesión del 18 de septiembre de 2026 en Bogotá / 19 de septiembre UTC.
Código validado: `5a8f9b368225372a80c5150a37d33737138216c9`, rama
`codex/android-build-validation`, base `3716f09e415c69f59102e74cffa6a1bbb154dec6`.
Las modificaciones posteriores de esta entrega son documentación de evidencia.

## Entorno y baseline

VERIFIED — Linux x86_64 WSL2, kernel `6.18.33.2-microsoft-standard-WSL2`;
Python de `.venv` 3.13.12; OpenJDK 21.0.11+10-1-Debian; Gradle 8.13;
AGP 8.13.2; SDK platform 36 revisión 2; Build Tools 35.0.0; libsignal 0.102.3;
desugar_jdk_libs 2.0.3. Docker 28.5.2+dfsg4; Python del contenedor 3.12.14.

VERIFIED — Git local y `git ls-remote origin refs/heads/main` coincidían con la base
indicada por el propietario. GitHub confirma repositorio privado y rama principal main.
No estaban definidas las variables AGENT_MEMORY_*; no se cargó ni sobrescribió memoria externa.
La referencia del handoff a una publicación aún pendiente estaba desactualizada.

VERIFIED — [Actions 35407207521](https://github.com/devopssolutionsia/umbra/actions/runs/35407207521)
corresponde exactamente a la base. Repository guard, relay/core y contenedor pasaron.
Android falló en setup-android: `Failed to find package 'tools'`. No llegó a compilar.
Se mantiene el SHA de la acción y se especifica `packages: platform-tools`.

VERIFIED — El primer test local, aunque activó `.venv`, descubrió que el Java global
era 25 y el entorno no estaba uniformemente seleccionado para Java 21: la comprobación
de sintaxis terminó con `release version 21 not supported`. Se repitió la suite completa
con JAVA_HOME y PATH seleccionando JDK 21. El primer preflight devolvió 2 por faltar
SDK 36. Gradle del sistema era 4.4.1; el script ahora selecciona exclusivamente 8.13.

## Comandos y resultados locales

VERIFIED — Antes de todas las suites Python se ejecutó `source .venv/bin/activate`.
Para Java se utilizó:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH="$JAVA_HOME/bin:$PATH"
source .venv/bin/activate
```

| Comando ejecutado | Resultado verificado |
|---|---|
| `bash scripts/test_local.sh` | Exit 0: 80 pytest backend, 20 escenarios CoreSelfTest y 85 SecuritySelfTest. Separadamente, 12 controles estáticos y sintaxis de 20 archivos Java. Repetido después de cambiar el código Android. |
| `python -m unittest discover -s scripts/tests -p 'test_*.py' -v` | Exit 0: 46 pruebas de herramientas; incluye 7 pruebas sintéticas del verificador de APK, no criptografía ni Android. |
| `python scripts/repository_guard.py --git-history` | Exit 0, sin patrones soportados ni nombres prohibidos; no constituye auditoría. |
| `python scripts/bootstrap_gradle.py` | Exit 0, descarga Gradle 8.13 con comprobación del checksum oficial. |
| `sdkmanager 'platforms;android-36' 'build-tools;35.0.0'` | Exit 0; instaló la plataforma faltante. |
| `python scripts/build_android.py --check-only` | Exit 0 tras preparar el entorno. |
| `gradle --no-daemon :app:dependencies --configuration connectedDebugRuntimeClasspath` | Exit 0 usando Gradle 8.13 desde android; resolución de libsignal-android/client 0.102.3. |
| `python scripts/build_android.py` | Exit 0: tests de ambas variantes, ambos APK debug, ambos lint, manifiestos y APK/JNI. |
| `apksigner verify --verbose <APK>` | Exit 0 en ambos APK debug; firma v2 verificada. No firma release. |

VERIFIED — Se ejecutaron 30 métodos de `SignalIntegrationTest` para connected y los
mismos 30 para offline: 0 failures, 0 errors, 0 skipped. JNI real Linux x86_64 de
libsignal-client; almacenamiento sintético MemoryRecords y org.json para JVM.
Incluyen ratchet/reinicio, alteración de ciphertext, atomicidad, duplicados,
rechazo de contactos no verificados y pruebas firmadas del protocolo cercano.
Ningún test fue eliminado, deshabilitado ni reemplazado por un mock criptográfico.

VERIFIED — Fallos corregidos durante la compilación:

- El AAR exigía core library desugaring, ausente en el módulo. Se habilita con versión fija.
- Lint rechazaba accesos Bluetooth sin comprobación explícita reconocible. Se comprueba
  BLUETOOTH_CONNECT y se captura específicamente SecurityException por revocación.
- Para GestureBackNavigation se documenta una excepción **solo** en el callback legacy:
  onCreate ya registra OnBackInvokedCallback nativo para API 33+, y API 31–32 necesita
  onBackPressed. No se desactiva lint globalmente ni se crea baseline.
- El watchdog pasa a scheduleWithFixedDelay para evitar ráfagas de tareas tras suspensión.
- Se mueve el texto señalado por lint a un recurso de strings.
- Se excluye JNI auxiliar de testing de Signal del APK, conservando JNI del protocolo en
  todas las ABI. El gate nuevo rechazó correctamente los APK anteriores que aún lo contenían;
  después se reconstruyó con la exclusión. No se excluyen tests JVM.

VERIFIED — Lint final: 0 errores y 4 advertencias por variante. Se mantienen visibles
UnusedAttribute (enableOnBackInvokedCallback desde API 33), GradleDependency
(desugar_jdk_libs) y dos NewerVersionAvailable (ZXing y org.json de tests).
No se actualizan indiscriminadamente las dependencias. Avisos de APIs Java deprecated
siguen visibles. Ver [procedimiento y fuentes](../ANDROID_BUILD.md).

## Backend y contenedor

VERIFIED — Se ejecutó `docker build --pull -t umbra-relay:p0-01 relay`, exit 0.
Imagen local `sha256:1536ceb84b2eef47675c2656e13cb0dfa3b833bd8bae6174f173078a32e23177`.
Se ejecutó el smoke de verify.yml, añadiendo la comprobación explícita del UID:

```bash
docker run --detach --name umbra-relay-p0-01 --network none --read-only \
  --tmpfs /tmp:rw,noexec,nosuid,size=32m \
  --tmpfs /data:rw,nosuid,uid=10001,gid=10001,mode=0700,size=64m \
  --cap-drop ALL --security-opt no-new-privileges \
  --pids-limit 128 --memory 256m umbra-relay:p0-01
docker exec umbra-relay-p0-01 python -c 'import os,urllib.request; assert os.getuid() == 10001; r=urllib.request.urlopen("http://127.0.0.1:8080/healthz",timeout=2); assert r.status == 200'
docker rm -f umbra-relay-p0-01
```

VERIFIED — Se usó el bucle de hasta 30 intentos de CI. El primer intento encontró el
servidor arrancando; el siguiente pasó con UID 10001 y HTTP 200. Limpieza por trap.
Sin publicación de puertos, despliegue ni datos reales. Los tags de imagen y locks
sin hashes siguen como deuda P0-09, no se promete reproducción binaria del contenedor.

## Límites

NOT_VERIFIED — No se ejecutaron instrumentación Android, navegación real, revocación
de permisos en un dispositivo, SQLite/Keystore Android, Bluetooth emulado/físico,
release/R8 ni auditoría independiente. No hay dos teléfonos. Estas tareas permanecen
abiertas; no se debilitó Keystore ni se añadió una variante lab o canal de red offline.

VERIFIED — La comprobación de APK demuestra empaquetado ELF de libsignal_jni.so en
arm64-v8a, armeabi-v7a, x86 y x86_64 y ausencia de JNI de escritorio/testing en las
entradas del ZIP. No demuestra ejecución de JNI en esas cuatro arquitecturas Android.
Offline carece de INTERNET y ACCESS_NETWORK_STATE; connected conserva ambos.
Los APK son debug con datos de prueba; no constituyen entrega de producción.

## Recompilación limpia y artefactos finales

VERIFIED — Se ejecutó `.umbra-tools/gradle-8.13/bin/gradle --no-daemon -p android clean`
(exit 0), seguido de `python scripts/build_android.py` (exit 0). Gradle informó
`BUILD SUCCESSFUL in 1m 53s`, 105 tareas ejecutadas, ninguna reutilizada como
UP-TO-DATE. Los tests reales volvieron a ejecutarse para ambas variantes.
La limpieza eliminó espacio residual de los ZIP incrementales después de retirar
JNI de testing: los APK finales pasan de unos 967 MiB a unos 443 MiB por variante.
El empaquetador conserva las bibliotecas nativas sin strip en este entorno; eso
explica su tamaño, y no demuestra compatibilidad de R8/release.

- VERIFIED — `android/app/build/outputs/apk/connected/debug/app-connected-debug.apk`: 463635637 bytes; SHA-256 `7f6708f28163f6c1a6fa4d559a1f71fab91e157f0cbaa2b1ef1a2b93420f84ce`. JUnit: 30 tests, 0 failures, 0 errors, 0 skipped; timestamp `2026-09-19T00:17:45.060Z`.
- VERIFIED — `android/app/build/outputs/apk/offline/debug/app-offline-debug.apk`: 463635585 bytes; SHA-256 `c6e9ff4b94c6f175d358c63a1b4ab827940e47be08089c122de1626302187d7a`. JUnit: 30 tests, 0 failures, 0 errors, 0 skipped; timestamp `2026-09-19T00:17:48.335Z`.

VERIFIED — Salidas registradas: [build limpio, permisos y hashes](2026-09-19-p0-01-build.txt),
[backend/núcleo](2026-09-19-p0-01-local-tests.txt) y [herramientas](2026-09-19-p0-01-tool-tests.txt).
Los reportes HTML/JUnit están en `android/app/build/reports/` y `test-results/`;
CI los conserva como artefactos privados durante siete días. Los APK no se añaden a Git.
No se promete igualdad bit a bit entre claves debug/máquinas diferentes.

NOT_VERIFIED — El estado CI de esta rama debe consultarse en la PR sobre su SHA exacto;
los resultados del baseline no se atribuyen a los cambios nuevos.
